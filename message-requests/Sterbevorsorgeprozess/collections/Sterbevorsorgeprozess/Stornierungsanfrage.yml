info:
  name: Stornierungsanfrage
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8080/engine-rest/message
  body:
    type: json
    data: |-
      {
        "messageName": "globalCancelRequest",
        "businessKey": "user@demo.org",
        "processVariables": {}
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
