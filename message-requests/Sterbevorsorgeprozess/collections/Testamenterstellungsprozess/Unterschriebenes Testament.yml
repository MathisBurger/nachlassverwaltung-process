info:
  name: Unterschriebenes Testament
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8080/engine-rest/message
  body:
    type: json
    data: |-
      {
        "messageName": "signedLastWill",
        "businessKey": "L1234567",
        "processVariables": {
          "signedLastWill": {"value": "Hier ist das signed Document", "type": "String"}
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
