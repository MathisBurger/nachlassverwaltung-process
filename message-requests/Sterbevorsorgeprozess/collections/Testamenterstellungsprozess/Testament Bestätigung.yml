info:
  name: Testament Bestätigung
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8080/engine-rest/message
  body:
    type: json
    data: |-
      {
        "messageName": "estateDocument",
        "businessKey": "user@demo.org",
        "processVariables": {
          "documentContent": {"value": "Hier mein Millionen Depot", "type": "String"}
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
