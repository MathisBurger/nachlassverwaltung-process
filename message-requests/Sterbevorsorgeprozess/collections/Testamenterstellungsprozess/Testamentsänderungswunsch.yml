info:
  name: Testamentsänderungswunsch
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/engine-rest/message
  headers:
    - name: Content-Type
      value: application/json
  auth: inherit

runtime:
  scripts:
    - type: before-request
      code: |-
        req.setBody({
          "messageName": "lastWillSplitChange",
          "businessKey": "L1234567",
          "processVariables": {
            "estateSplit": {"value": JSON.stringify({
            "estateObj1": {
                "hugo.habicht@demo.org": 75,
                "sugi.sorglos@demomail.org": 25
            }
        }), "type": "Json"}
          }
        })

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
