info:
  name: Änderungswunsch
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/engine-rest/message
  body:
    type: json
    data: |-
      {
        "messageName": "funeralPlanningChange",
        "businessKey": "user@demo.org",
        "processVariables": {
          "funeralType": {"value": "burial", "type": "String"},
          "funeralObject": {"value": "urn", "type": "String"},
          "maxAmountGuests": {"value": 12, "type": "Integer"},
          "budget": {"value": 1000, "type": "Integer"}
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
