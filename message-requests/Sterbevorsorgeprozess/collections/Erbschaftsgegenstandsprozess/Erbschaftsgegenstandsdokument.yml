info:
  name: Erbschaftsgegenstandsdokument
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/engine-rest/message
  body:
    type: json
    data: |-
      {
        "messageName": "estateUpdate",
        "businessKey": "user@demo.org",
        "processVariables": {
          "estateDocument": {"value": "Hier mein Millionen Depot", "type": "String", "valueInfo": {"transient": true}}
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
