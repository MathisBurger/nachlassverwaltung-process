info:
  name: Erbschaftsgegenstandsdokument
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/engine-rest/message
  body:
    type: json
    data: |-
      {
        "messageName": "estateDocument",
        "businessKey": "L1234567",
        "processVariables": {
          "title": {"value": "Depot 1", "type": "String"},
          "estateDocument": {"value": "Hier mein Millionen Depot mit 20 Mio", "type": "String"}
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
