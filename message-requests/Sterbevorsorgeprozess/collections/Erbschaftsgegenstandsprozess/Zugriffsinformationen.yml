info:
  name: Zugriffsinformationen
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/engine-rest/message
  body:
    type: json
    data: |-
      {
        "messageName": "accessInformation",
        "businessKey": "L1234567",
        "processVariables": {
          "accessInformation": {"value": "Hier meine Bank Zugangsdaten", "type": "String"}
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
