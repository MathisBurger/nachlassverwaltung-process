info:
  name: Nächste Aktion
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8080/engine-rest/message
  body:
    type: json
    data: |-
      {
        "messageName": "estateProcessNextAction",
        "businessKey": "user@demo.org",
        "processVariables": {
          "continueCreateNewEstate": {"value": false, "type": "Boolean"}
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
