info:
  name: Split Zustimmung
  type: http
  seq: 7

http:
  method: POST
  url: http://localhost:8080/engine-rest/message
  body:
    type: json
    data: |-
      {
        "messageName": "splitAgree",
        "businessKey": "L1234567",
        "correlationKeys": {
          "email": {"value": "hugo.habicht@demomail.org", "type": "String"}
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
