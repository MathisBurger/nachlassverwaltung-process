info:
  name: Split Zustimmung
  type: http
  seq: 6

http:
  method: POST
  url: http://localhost:8080/engine-rest/message
  body:
    type: json
    data: |-
      {
        "messageName": "splitAgreement",
        "businessKey": "L1234567",
        "localCorrelationKeys": {
          "email": {"value": "hugo.habicht@demomail.org", "type": "String"}
        },
        "processVariables": {
            "agreed": {"value": true, "type": "Boolean"}
          }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
