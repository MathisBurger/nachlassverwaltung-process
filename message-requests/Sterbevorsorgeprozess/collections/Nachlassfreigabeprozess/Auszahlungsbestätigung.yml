info:
  name: Auszahlungsbestätigung
  type: http
  seq: 10

http:
  method: POST
  url: http://localhost:8080/engine-rest/message
  body:
    type: json
    data: |-
      {
        "messageName": "cashoutConfirmation",
        "businessKey": "L1234567"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
