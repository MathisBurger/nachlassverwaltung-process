info:
  name: Split Ablehnung
  type: http
  seq: 8

http:
  method: POST
  url: http://localhost:8080/engine-rest/message
  body:
    type: json
    data: |-
      {
        "messageName": "splitDisagree",
        "businessKey": "L1234567",
        "correlationKeys": {
          "email": {"value": "mab8881@thi.de", "type": "String"}
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
