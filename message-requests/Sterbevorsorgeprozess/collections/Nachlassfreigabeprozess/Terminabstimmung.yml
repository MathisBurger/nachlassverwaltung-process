info:
  name: Terminabstimmung
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:8080/engine-rest/message
  body:
    type: json
    data: |-
      {
        "messageName": "appointmentVote",
        "businessKey": "L1234567",
        "correlationKeys": {
          "email": {"value": "", "type": "String"}
        },
        "processVariables": {
          "vote": {"value": "", "type": "String", "valueInfo": {"transient": true}}
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
