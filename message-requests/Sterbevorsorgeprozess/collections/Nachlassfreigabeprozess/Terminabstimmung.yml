info:
  name: Terminabstimmung
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8080/engine-rest/message
  body:
    type: json
    data: |-
      {
        "messageName": "appointmentVote",
        "businessKey": "L1234567",
        "localCorrelationKeys": {
          "email": {"value": "susi.sorglos@demomail.org", "type": "String"}
        },
        "processVariables": {
          "vote": {"value": "2026-08-14T09:30:00Z", "type": "String"}
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
