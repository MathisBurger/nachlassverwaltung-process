info:
  name: Neue Einigung
  type: http
  seq: 7

http:
  method: POST
  url: http://localhost:8080/engine-rest/message
  body:
    type: json
    data: |-
      {
        "messageName": "newSplitAgreement",
        "businessKey": "L1234567",
        "processVariables": {
          "splitInfo": {"value": "Hier ist die neue Einigung", "type": "String"}
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
