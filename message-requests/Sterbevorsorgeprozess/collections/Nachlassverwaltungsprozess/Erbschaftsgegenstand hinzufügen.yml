info:
  name: Erbschaftsgegenstand hinzufügen
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:8080/engine-rest/message
  body:
    type: json
    data: |-
      {
        "messageName": "estateUpdate",
        "businessKey": "L1234567",
        "processVariables": {
          "titleTransient": {"value": "Title", "type": "String", "valueInfo": {"transient": true}},
          "estateDocumentTransient": {"value": "Hier mein Millionen Depot mit 80 Mio", "type": "String", "valueInfo": {"transient": true}}
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
