info:
  name: Letzen Worte Update
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:8080/engine-rest/message
  body:
    type: json
    data: |-
      {
        "messageName": "lastWordsChange",
        "businessKey": "user@demo.org",
        "processVariables": {
          "preference": {"value": false, "type": "Boolean"}
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
