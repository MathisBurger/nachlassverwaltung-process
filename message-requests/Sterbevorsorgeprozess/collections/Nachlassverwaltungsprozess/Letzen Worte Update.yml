info:
  name: Letzen Worte Update
  type: http
  seq: 8

http:
  method: POST
  url: http://localhost:8080/engine-rest/message
  body:
    type: json
    data: |-
      {
        "messageName": "lastWordsChange",
        "businessKey": "L1234567",
        "processVariablesLocal": {
          "preference": {"value": true, "type": "Boolean", "valueInfo": {"transient": true}}
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
