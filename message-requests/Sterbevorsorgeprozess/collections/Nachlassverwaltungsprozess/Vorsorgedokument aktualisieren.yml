info:
  name: Vorsorgedokument aktualisieren
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:8080/engine-rest/message
  body:
    type: json
    data: |-
      {
        "messageName": "updateAdvanceDirective",
        "businessKey": "user@demo.org",
        "processVariables": {
          "documentId": {"value": "powerOfAttorney", "type": "String", "valueInfo": {"transient": true}}
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
