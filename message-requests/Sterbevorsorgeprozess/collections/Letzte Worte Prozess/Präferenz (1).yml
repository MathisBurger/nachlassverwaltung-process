info:
  name: Präferenz (1)
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/engine-rest/message
  body:
    type: json
    data: |-
      {
        "messageName": "lastWords",
        "businessKey": "user@demo.org",
        "processVariables": {
          "lastWords": {"value": "Meine letzten Worte!", "type": "String"}
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
