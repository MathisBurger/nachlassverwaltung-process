info:
  name: Identitätsantwort
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/engine-rest/message
  body:
    type: json
    data: |-
      {
        "messageName": "identifyCheckResponse",
        "businessKey": "user@demo.org",
        "processVariables": {
          "identityValid": {"value": true, "type": "Boolean"}
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
