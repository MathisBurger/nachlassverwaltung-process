info:
  name: Beglaubigungsablehnung
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/engine-rest/message
  body:
    type: json
    data: |-
      {
        "messageName": "advanceDirectiveContent",
        "businessKey": "user@demo.org",
        "correlationKeys": {
          "documentId": {"value": "hugo.habicht@demomail.org", "type": "String"}
        },
        "processVariables": {
          "documentContent": {"value": "Gefällt mir nicht", "type". "String"}
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
