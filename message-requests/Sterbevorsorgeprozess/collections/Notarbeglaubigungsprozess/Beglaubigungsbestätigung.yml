info:
  name: Beglaubigungsbestätigung
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/engine-rest/message
  body:
    type: json
    data: |-
      {
        "messageName": "documentNotarized",
        "businessKey": "user@demo.org",
        "correlationKeys": {
          "documentId": {"value": "hugo.habicht@demomail.org", "type": "String"}
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
